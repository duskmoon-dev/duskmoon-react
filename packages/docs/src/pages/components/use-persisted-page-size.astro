---
import ComponentDocPage from "../../components/ComponentDocPage.astro";
import { getComponentDoc } from "../../lib/component-docs";

const component = getComponentDoc("use-persisted-page-size");

if (!component) {
  throw new Error("Missing component docs for use-persisted-page-size");
}
---

<ComponentDocPage component={component} />
